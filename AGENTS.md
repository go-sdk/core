# 仓库协作规范

## 文档优先

每次修改代码前，必须先阅读以下文档：

1. `AGENTS.md`
2. `PROJECT_MAP.md`
3. `README.md`

如果文档描述与实际代码不一致，应在同一次修改中自动更新相关文档，确保文档反映最终实现。

## 修改原则

- 这是个人 Go 基础类库，优先保持 API 简洁、行为统一和向后兼容。
- 修改前检查目录结构、调用关系和 Git 工作区状态，不覆盖或混入无关改动。
- 遵循现有包划分和代码风格，优先局部修改，不引入缺少明确收益的依赖或抽象。
- 代码注释、维护文档和面向使用者的说明统一使用简体中文。
- 注释只描述最终设计意图、行为和关键约束，不记录修改历史，不复述代码。
- `logx/log.go` 直接来源于 zerolog 上游实现，保持原样，不修改该文件。

## 设计约束

- `logx` 负责进程级全局日志，允许在包初始化时建立默认配置，并在应用读取配置后再次调用 `Init`。
- 日志按进程统一管理，不维护独立 Logger 的资源生命周期。
- `seq` 的 Snowflake 生成器使用固定机器编号，约束为同一时间仅运行一个生成器实例。
- `seq` 在包初始化时启用 UUID 随机池，这是本类库的既定性能策略。
- `osx.GetEnv` 以第一个已设置的环境变量为准；即使为空或转换失败也返回转换结果，仅在全部未设置时返回默认值。

## 验证边界

- 修改后使用项目已有的格式化工具，并检查 `git diff`、`git diff --check` 和工作区状态。
- 可以执行仅用于确认编译的命令，但不得隐式运行测试、启动程序或访问外部系统。
- 未经明确要求，不运行单元测试、集成测试、示例程序、服务或任何外部接口。
- 不执行部署、发布、上传或 `git push`；本地提交需要用户明确要求。

## 提交规范

- 提交信息遵循 Conventional Commits。
- 标题使用简洁英文；需要补充背景、影响或风险时写入提交正文。
- 只提交当前任务明确包含并已复核的文件。
